<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".SplashScreenActivity">

    <ImageView
        android:id="@+id/background_top_left"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/shape_top_left"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="fitXY"
        android:alpha="0.2"
        android:contentDescription="Dekorasi atas kiri" />

    <ImageView
        android:id="@+id/background_bottom_right"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/shape_bottom_right"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:scaleType="fitXY"
        android:alpha="0.2"
        android:contentDescription="Dekorasi bawah kanan" />

    <ImageView
        android:id="@+id/logo_foca"
        android:layout_width="250dp" android:layout_height="250dp" android:src="@drawable/foca_logo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="fitCenter" android:contentDescription="Foca Logo Katering" />

    <ImageView
        android:id="@+id/bottom_wave_image"
        android:layout_width="0dp"
        android:layout_height="wrap_content" android:src="@drawable/garis_lengkung_loading"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:scaleType="fitXY" android:adjustViewBounds="true" android:contentDescription="Garis lengkung bawah" />

</androidx.constraintlayout.widget.ConstraintLayout>